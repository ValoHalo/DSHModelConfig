diff --git a/packages/client/ui-dsh-model-config/src/host-models/client/ModelListEditor.tsx b/packages/client/ui-dsh-model-config/src/host-models/client/ModelListEditor.tsx
--- a/packages/client/ui-dsh-model-config/src/host-models/client/ModelListEditor.tsx
+++ b/packages/client/ui-dsh-model-config/src/host-models/client/ModelListEditor.tsx
@@ -12,7 +12,7 @@
  * rows the user can still fill in by hand.
  */
 
-import { useState } from 'react'
+import { useContext, useRef, useState } from 'react'
 import type { ReactNode } from 'react'
 import type { LlmDiscoveredModel } from '@deepseek-ai/dsh-api-remotes/client'
 import { Button, Modal } from '@deepseek-ai/dsh-client-ui-primitives'
@@ -20,5 +20,7 @@ import { formatCapacity, parseCapacity } from './DeepSeekModelsEditor.tsx'
 import type { DeepSeekModelDraft } from './DeepSeekModelsEditor.tsx'
 import type { en } from './locales.ts'
+import { ModelCapabilitySlotContext } from '../../client/model-capability-slot.ts'
+import { withModelCapacityDefaults } from '../../client/model-capacity-defaults.ts'
 import styles from './ModelsSection.module.css'
 
 /**
@@ -143,14 +145,14 @@ function capacitySpelling(value: number | undefined): string {
   return value === undefined ? '' : formatCapacity(value)
 }
 
-/** Adopt a candidate, keeping whatever capacities the provider disclosed. */
+/** Adopt a candidate, filling capacities from same-id catalog data or route fallbacks. */
 function adopt(candidate: LlmDiscoveredModel): ModelDraft {
-  return {
+  return withModelCapacityDefaults({
     id: candidate.id,
     ...candidate.name === undefined ? {} : { name: candidate.name },
     ...candidate.contextWindow === undefined ? {} : { contextWindow: candidate.contextWindow },
     ...candidate.maxTokens === undefined ? {} : { maxTokens: candidate.maxTokens },
-  }
+  })
 }
 
 /**
@@ -165,7 +167,23 @@ export function ModelListEditor(props: ModelListEditorProps): ReactNode {
   const [failure, setFailure] = useState<string | undefined>(undefined)
   const [candidates, setCandidates] = useState<readonly LlmDiscoveredModel[] | undefined>(undefined)
   const [picked, setPicked] = useState<ReadonlySet<string>>(new Set())
+  const renderModelCapability = useContext(ModelCapabilitySlotContext)
+  const rowKeys = useRef(new WeakMap<ModelDraft, number>())
+  const nextRowKey = useRef(0)
+
+  const rowKey = (model: ModelDraft): number => {
+    const existing = rowKeys.current.get(model)
+    if (existing !== undefined) return existing
+    const created = nextRowKey.current++
+    rowKeys.current.set(model, created)
+    return created
+  }
+
+  const inheritRowKey = (previous: ModelDraft, next: ModelDraft): ModelDraft => {
+    rowKeys.current.set(next, rowKey(previous))
+    return next
+  }
   const [candidateQuery, setCandidateQuery] = useState('')
   // Rows carry an id and a name; capacities are the exception, so they stay
   // folded until asked for rather than crowding every row with four inputs.
   const [expanded, setExpanded] = useState<ReadonlySet<number>>(new Set())
@@ -225,8 +243,8 @@ export function ModelListEditor(props: ModelListEditorProps): ReactNode {
       const cleared = new Set(
         Object.entries(next).filter(([, value]) => value === undefined || value === '').map(([key]) => key),
       )
-      return Object.fromEntries(
+      return inheritRowKey(model, Object.fromEntries(
         Object.entries({ ...model, ...next }).filter(([key]) => !cleared.has(key)),
-      )
+      ))
     }))
   }
@@ -335,7 +353,7 @@ export function ModelListEditor(props: ModelListEditorProps): ReactNode {
       </div>
       {models.length === 0 ? <p className={styles['modelEmpty']}>{t('modelsEmpty')}</p> : null}
       {models.map((model, index) => (
-        <div key={index} className={styles['modelEntry']}>
+        <div key={rowKey(model)} className={styles['modelEntry']}>
           <div className={styles['modelRow']}>
             <input
               className={styles['input']}
@@ -417,6 +435,20 @@ export function ModelListEditor(props: ModelListEditorProps): ReactNode {
                     onChange={(event) => { editCapacity(index, 'maxTokens', event.target.value) }}
                   />
                 </label>
+                {renderModelCapability === undefined
+                  ? null
+                  : (
+                    <div className={styles['modelCapabilities']}>
+                      {renderModelCapability({
+                        model,
+                        position: index + 1,
+                        ...probe.api === undefined ? {} : { protocol: probe.api },
+                        disabled,
+                        onChange: next => onChange(models.map((entry, at) =>
+                          at === index ? inheritRowKey(entry, next) : entry)),
+                      })}
+                    </div>
+                  )}
               </div>
             )
             : null}
diff --git a/packages/client/ui-dsh-model-config/src/host-models/client/ModelsSection.module.css b/packages/client/ui-dsh-model-config/src/host-models/client/ModelsSection.module.css
--- a/packages/client/ui-dsh-model-config/src/host-models/client/ModelsSection.module.css
+++ b/packages/client/ui-dsh-model-config/src/host-models/client/ModelsSection.module.css
@@ -1,3 +1,8 @@
+.modelCapabilities {
+  grid-column: 1 / -1;
+  min-width: 0;
+}
+
 /* Every color resolves through a `--dsw-alias-*` token. Bare `--border` /
  * `--surface` / `--text-*` names, which nothing in this app defines, would
  * render the light-mode literals written as their fallbacks and stay light
diff --git a/packages/client/ui-dsh-model-config/src/host-models/client/ModelsSection.tsx b/packages/client/ui-dsh-model-config/src/host-models/client/ModelsSection.tsx
--- a/packages/client/ui-dsh-model-config/src/host-models/client/ModelsSection.tsx
+++ b/packages/client/ui-dsh-model-config/src/host-models/client/ModelsSection.tsx
@@ -25,6 +25,7 @@
 import type { SettingsSchemaOperations } from './schema-operations.ts'
 import { ProviderEditor, type ProviderEditorProps } from './ProviderEditor.tsx'
 import type { en } from './locales.ts'
+import { MODEL_CAPABILITY_SLOT, ModelCapabilitySlotContext } from '../../client/model-capability-slot.ts'
 import styles from './ModelsSection.module.css'
-
+
 /** Injected dependencies of {@link ModelsSection} (slot `inject`). */
@@ -49,4 +50,4 @@
 /** The child slots this section declares and dispatches (see ./slot-contract.ts). */
-type ModelsChildSlots = 'settings.models.provider-card' | 'settings.models.footer'
+type ModelsChildSlots = 'settings.models.provider-card' | 'settings.models.footer' | typeof MODEL_CAPABILITY_SLOT
-
+
 /** The child-slot dispatch function the renderer binds for the section. */
@@ -211,6 +212,10 @@
     || schema === undefined || t === undefined
   ) return null
-  return <Loaded injected={{ controller, useSnapshot, operations, schema, t }} renderSlot={renderSlot} />
+  return (
+    <ModelCapabilitySlotContext.Provider value={owner => renderSlot(MODEL_CAPABILITY_SLOT, owner)}>
+      <Loaded injected={{ controller, useSnapshot, operations, schema, t }} renderSlot={renderSlot} />
+    </ModelCapabilitySlotContext.Provider>
+  )
 }
-
+
 function Loaded({ injected, renderSlot }: { injected: ModelsSectionFace; renderSlot: ModelsRenderSlot }): ReactNode {
diff --git a/packages/client/ui-dsh-model-config/src/host-models/client/index.ts b/packages/client/ui-dsh-model-config/src/host-models/client/index.ts
--- a/packages/client/ui-dsh-model-config/src/host-models/client/index.ts
+++ b/packages/client/ui-dsh-model-config/src/host-models/client/index.ts
@@ -34,4 +34,5 @@
 import { en, zh, type ModelsKey } from './locales.ts'
 import { WELCOME_NOTICE_SETTINGS_NAMESPACE } from '../onboarding-copy.ts'
+import { MODEL_CAPABILITY_SLOT } from '../../client/model-capability-slot.ts'
-
+
 export type { ModelsSectionInjected, ModelsSectionProps } from './ModelsSection.tsx'
@@ -146,4 +147,5 @@
     children: {
       'settings.models.provider-card': { kind: 'keyed', scope: 'root' },
       'settings.models.footer': { kind: 'list', scope: 'root' },
+      [MODEL_CAPABILITY_SLOT]: { kind: 'list', scope: 'root' },
     },
